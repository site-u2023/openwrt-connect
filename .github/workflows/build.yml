name: Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build
        shell: cmd
        run: openwrt-connect-build.bat

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: openwrt-connect
          path: |
            openwrt-connect.exe
            openwrt-connect.msi
